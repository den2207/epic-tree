<!-- BEGIN epic-tree adapter — generated block; epic-new replaces everything between these markers, never edit by hand -->
# AGENTS.md — epic-tree adapter for non-Claude agents

contract: generated by `epic-new` at the epic root / read by any AI agent that does not run the Claude Code hook+skills (Codex, Cursor, Copilot CLI, …) / composes with other AGENTS.md files: the nearer file refines this one / delimited by BEGIN/END epic-tree markers — regeneration replaces only this block, text outside the markers is never touched / the block (only) is removed when the epic closes

Epics live in this workspace. Before doing ANY work here:

1. List `epics/*/` (skip `_archive/`) — every directory with a `state.md` is a live
   epic. Your task names the epic slug (usually as `epic <slug>: …`); if it names
   none and the work is clearly epic work, ask — never guess. Check the charter's
   repo topology: if your working repo is not in it, that epic does not apply to you.
2. Read `epics/<slug>/charter.md` — the **Non-negotiables** section is binding
   for you verbatim. The roles table defines your role and write scope.
3. Read `epics/<slug>/state.md` (where the work stands) and the row of the
   active slice in `plan.md`. Do not read the whole plan or all journals.
4. Create YOUR journal file before changing anything: `epics/<slug>/journal/
   <YYYY-MM-DD>-<HHMM>-<role>-<unique-token>.md` — the token is mandatory (e.g. 4
   random hex chars): two same-role sessions starting the same minute must never
   race for one filename. Copy the section structure of any existing entry;
   never reuse a file. Exception:
   for an explicitly read-only review task, create and modify nothing, and state in
   your report that journaling was intentionally skipped.
5. Write scope — the restriction below applies INSIDE `epics/` only; editing
   the active slice's source files in the member repos is your normal work:
   - **executor**: within `epics/`, write ONLY your own journal file; put
     ledger/state/gate suggestions in its Proposals section.
   - **coordinator**: fixed handoff order — journal → ledger → gated-actions →
     plan.md statuses (evidence required) → `state.md` rewritten LAST (max 40 lines,
     `updated: <ISO ts> by coordinator`).
6. Persistence: keep working until the active slice's verify command passes or you hit
   a named gate (G-NN) or a concrete blocker — do not stop merely to ask for
   confirmation. Record exact evidence (verify output, commit hashes) in your journal
   before exit. Work that is not in a journal did not happen.

Slice statuses flip to done only with evidence (verify-command output or commit hash).
Human-only actions live in `gated-actions.md` (G-NN) — never execute them yourself.
When a G-NN is not in `gated-actions.md`, look in `gated-actions.ARCHIVE.md` (closed
blocks move there at handoff).
<!-- END epic-tree adapter -->
