# gated actions — <epic-slug>

contract: queue of actions only the HUMAN may execute (cloud writes, logins, manual IDE steps) / written by the coordinator, executed by the human in their own terminal / gate POLICY lives in charter Non-negotiables; this file is the live queue / blocks done before the current session are moved to `gated-actions.ARCHIVE.md` at handoff (G-NN headings preserved)

<!-- One block per action. Command must be copy-paste ready: cwd, account, expected output. -->
## G-01 — <title> (pending|done <date>)

- cwd: <absolute path>
- account/project: <identity this runs under>
- command:

```bash
<exact command>
```

- expected: <what success looks like>
